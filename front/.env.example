# Environment variables (example)
# Copy to .env.local and fill in.

# 개발: 설정 불필요 (Vite proxy가 /api → localhost:8000 처리)
# 홈서버 배포: nginx 리버스 프록시 없이 직접 연결할 경우에만 설정
# VITE_API_BASE_URL=http://<홈서버 IP>:8000

# mock 모드 강제 활성화 (백엔드 없이 테스트할 때만 사용)
# VITE_USE_MOCK=true

VITE_CHAT_STREAM_TIMEOUT_MS=30000
