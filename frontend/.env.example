# Frontend Environment Variables
# Copy to .env.local and fill in.

# 백엔드 API base URL
# 개발: Vite proxy(/api → localhost:8000)를 사용하므로 설정 불필요
# 홈서버 배포: nginx 리버스 프록시 없이 직접 연결할 경우 설정
# VITE_API_BASE_URL=http://<홈서버 IP>:8000
